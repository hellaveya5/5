```
mkdir learngit //创建并打开
cd learngit//打开
pwd
git init
git add
git commit -m ""//提交信息
git reset --hard HEAD^//版本回退
git reset --hard (num)//版本到。。
cat readme.txt
git reflog
git flog
git checkout --<file>
rm <文件名>
ls//查看库中文件
git switch<name>||git checkout <name>//切换分支
git branch
git branch <name>
git checkout -b <name>||git switch -c <name>//创造并切换分支
从远程库中克隆：git clone git@github.com:<userName>/<rspositoryName>.git
```

